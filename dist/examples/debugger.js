'use strict';

// Get the button which will start the debugger
var debuggerButton = document.querySelector('.js-debugger-button');

// When the button is tapped, hit a breakpoint and open the debugger
debuggerButton.addEventListener('click', function() {
	debugger;
});
